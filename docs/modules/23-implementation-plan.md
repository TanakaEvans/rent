# Module 23 - Implementation Plan (Build Order, Party Impact, Coupling, Testing)

> The authoritative build plan for Dzimba. It answers three questions: **which module is built first**, **who in the system is impacted and related to each module**, and **which modules are so tightly coupled they must be built together**. Every module is built **end to end** and **tested after every single feature**.

## 1. Build Philosophy (non-negotiable)

1. **Vertical slices, end to end.** A module is never developed "top to bottom in one sweep" and it is never left half-built. Each feature within a module is cut from **database → model → service/controller → route → Inertia page → shared component → tests** in one sitting.
2. **Coupled modules ship together.** Modules that share state, a shared pattern, or a continuous workflow are developed inside the same build wave. Splitting them produces broken intermediate systems and expensive rework.
3. **Test after every feature, not after the module.** The moment a feature is functional, its tests exist. A module is never marked complete with untested features.
4. **No loose ends.** Dead routes, orphaned pages, TODO markers, unused imports, unguarded routes and unseeded demo data are failures, not backlog items.
5. **One city first.** Everything is built for Harare residential rentals first. Multi-city and commercial roll out in later phases.
6. **Configuration-driven, not code-driven.** Every commercial/billing rule (Module 24) is data read through `ConfigurationService` — grace period, proration mode, suspension behaviour, invoice/receipt numbering, cycle-day map, renewal reminders, payment/POP/approval, late fees and featured pricing. Staff change them in the Configuration Centre; developers are not needed for pricing or policy changes.

## 1.5 Current Status & Next Up (MANDATORY - update after every feature)

> **Rule: after every finished feature, this section is updated in the same sitting. This document is the single source of truth for "what is built, what is in flight, what is next".** A feature or wave is NOT done until the tracker below is current.

| Field | Value |
|---|---|
| Current wave | Wave 5 - Operate (**M10 + M11**) |
| Wave status | IN PROGRESS (Wave 4 - Monetise fully DONE) |
| Design directive | **Configuration-driven, not code-driven** (`docs/modules/24-system-configuration.md` + `docs/modules/Staff should not need developers to chan.md`): every commercial rule (prices, grace, proration, suspension behaviour, invoice/receipt numbering, cycle days, renewal reminders, payment/POP/approval, late fees, featured pricing) is configuration read through `ConfigurationService` — never a code constant. |
| Completed slices | (Wave 1 - List & Discover DONE; Wave 2 - Engage DONE: Enquiries, Viewing slots, Viewing requests, Notifications in-app; Wave 3 - Commit DONE: Applications, Lease create, Lease signing, Lease renew, Document store. See the rows below for details.) |
| Completed slices | **Enquiry create/inbox (M5) - DONE**: `enquiries` table (message ≤1000, optional phone, reply, replied_at, read_at, status new/read/replied/closed + explicit transitions) → `EnquiryService` (create = available-only + open-thread anti-duplicate, method open/read, reply, close, inbox grouped by property, tenant list) → `EnquiryController` + 6 routes (`owner.enquiries.{index,show,reply,close}`, `tenant.enquiries.{index,store}`) → Owner inbox (grouped by property, to-count, new-highlight) + thread page (reply box, close, tenant/property context) + Tenant threads page + enquiry form on public detail page (tenant-only, guest→login) + nav/dashboard entries for both roles → seeded demo enquiry. Tests: create/validation/duplicate-block/404-unavailable; owner inbox isolation; status transitions new→read (on open) & replied (on reply) & closed; reply/close on closed blocked; tenant isolation (15 tests, `EngageTest`), access matrix extended (guest→login, tenant/owner 403s). |
| Completed slices | **Viewing slots (M6) - Wave 2 slice 2 DONE**: `viewing_slots` table (property_id FK, owner_id FK, starts_at/ends_at nullable-timestamp, status available/taken) → `ViewingSlot` model + `ViewingSlotService` (slotsFor with owner guard, create/update/destroy each authorize owner + slot↔property match, 404 on foreign rows) → `ViewingSlotController` + 4 routes (`owner.viewing-slots.{index,store,update,destroy}`) validated `starts_at after:now`, `ends_at after:starts_at` → Owner/ViewingSlots/Index page (create form, slot cards with is_past expiry grey-out, inline edit, delete; entry button on owner property show) → 3 seeded demo slots on first listed property → `StatusBadge` `expired`/`taken` tones. Tests: create/validation (past start, ends-before-start)/update/delete, owner isolation 404 (index+create), page lists only this property's slots; access matrix extended (guest→login, tenant 403, cross-owner mixin). |
| Completed slices | **Viewing requests (M6) - Wave 2 slice 3 DONE**: `viewing_requests` table (property_id, tenant_id, slot_id restricted-delete, request_message ≤1000, status requested/accepted/rescheduled/declined/completed/cancelled/no-show, outcome) → `ViewingRequest` state machine (`TRANSITIONS` per status, `TERMINAL` set) → `ViewingRequestService` (create guard: available property 404, slot-on-property, bookable slot, per-tenant open-request dedupe; accept locks slot→taken + double-book 409; decline; reschedule→rescheduled awaits tenant confirm; confirm locks new slot; cancel by either party releases a locked slot; complete; no-show releases slot; owner isolation 404 + transition guard 409) → `ViewingRequestController` + 11 routes (`owner.viewings.{index,accept,decline,reschedule,complete,no-show,cancel}`, `tenant.viewings.{index,store,confirm,cancel}`) → public detail page shows available slots to tenants with a Book a Viewing form → `Owner/Viewings/Index` (accept/decline, propose-another-slot picker, complete/no-show/cancel) + `Tenant/Viewings` (confirm rescheduled, cancel) → both navs + owner dashboard Pending Viewings stat + seeded demo request → StatusBadge requested/accepted/rescheduled/declined/completed/no-show tones. Tests: create/validation/409s (taken slot, double-book, terminal transition), accept locks, decline, reschedule→confirm, cancel releases slot, no-show/complete, full isolation (owner×owner, tenant×tenant), owner page lists only own (18 new, `EngageTest`), access matrix extended. |
| Completed slices | **Notifications in-app (M15 start) - Wave 2 slice 4 DONE** (WAVE 2 COMPLETE): framework `notifications` table added via `2026_09_09_000010` (uuid id, type, morphs notifiable, data json, read_at) → 5 notification classes (`NewEnquiryNotification`, `EnquiryRepliedNotification`, `ViewingRequestedNotification`, `ViewingAcceptedNotification`, `ViewingRescheduledNotification`) each `via=['database']` with `{title, body, link}` deep-links (enquiry→owner thread/show, viewing→party list pages) → fired inside `EnquiryService` (create→owner, reply→tenant) and `ViewingRequestService` (create→owner, accept→tenant, reschedule→tenant; confirm/cancel/decline deliberately silent) → `NotificationController` (index/read/readAll; read verifies `notifiable_id`=user, marks read, redirects to payload link) + 3 routes in the app-wide auth group (`notifications.index/read/read-all`) → shared props `notifications` (latest 8) + `unreadNotificationsCount` → header Bell with unread badge + dropdown (click→read+deep-link, View all) → `Notifications/Index` page (facade, read actions, mark-all-read) → seeded demo notifications (enquiry + viewing request, with data titles). Tests: each event raises the right row to the right recipient (Notification::fake assertions), unread count + payload title, read→deep-link redirect + read_at set, 404 on another user's notification, read-all clears, guest→login; access matrix (any authenticated role ok, guest redirects). |
| Completed slices | **Rental Applications (M7) - Wave 3 slice 1 DONE**: `rental_applications` expanded (`2026_09_09_000011` adds `reject_reason` varchar(1000) + `reviewed_by` FK — columns awaited from the Phase-2 spec) → `RentalApplication` state machine (`TRANSITIONS`: pending↔shortlisted, pending/shortlisted→approved/rejected; approved/rejected terminal) → 3 notification classes (`NewApplicationNotification`→owner on apply, `ApplicationApprovedNotification`/`ApplicationRejectedNotification`→tenant on decision) → `ApplicationService` (create: available-only + one-active-per-tenant-per-property dedupe, re-apply allowed after rejection; approve: sole-approved-per-property exclusivity, sets reviewed_by, **property status stays available — handover deferred to the lease slice**; toggleShortlist; reject: reason required + recorded) → `ApplicationController` + 6 routes (`owner.applications.{index,approve,shortlist,reject}`, `tenant.applications.{index,store}`) → public detail page Apply section (tenant: form or active-application state badge; guest→login) → `Tenant/Applications` (status prose, reject note, view listing) + `Owner/Applications/Index` (grouped by property, inline shortlist/approve/reject-with-reason) + both navs → seeded demo application notifies the owner. Tests (new `CommitTest`, 16): apply/validation/dedupe/re-apply-after-reject/404-unavailable, owner-notified, tenant page own-only, owner page grouped+scoped, approve-recorded-and-inert, cross-owner 404, one-approved-per-property, terminal immutability (approve-then-reject, reject-then-shortlist), shortlist toggle, reject reason required/recorded + tenant notified, guest redirect; access matrix extended (owner review ok / tenant 403, tenant apply ok / owner 403, guest→login). Full suite 138 passed. |
| Completed slices | **Lease create (M8) - Wave 3 slice 2 DONE**: `leases` + `lease_history` tables (`2026_09_09_000012`) → `Lease` state machine (draft→sent→signed→active→renewed→terminated, terminated terminal) + `LeaseHistory` audit + `Property::leases()` → `LeaseService` (`createFromApplication`: **approved-only** guard, one-lease-per-application/property, prefills tenant/property/rent/deposit from the application, unique `lease_no` (`LSE-<year>-NNNN`), default monthly payment terms, `clause_version 1`, history row `lease_created`, **property → `reserved` via PropertyService + history**, **auto-rejects remaining active applicants with "A lease has been created for another applicant." + notification** (the FR-05 handover deferred from slice 1), notifies tenant `LeaseCreatedNotification`; `listForOwner` scoped via `whereHas` property; `listForTenant`) → `LeaseController` + 3 routes (`owner.applications.lease` POST, `owner.leases.index`, `tenant.leases.index`) → `Owner/Leases/Index` (tenant, term, money terms, clause, status badge) + `Tenant/Leases` (status prose per state, owner/term/money/location cards) + "Create lease" CTA on the approved application card (hidden once `leases_count > 0`) + nav entries for both roles + StatusBadge `sent/signed/renewed/terminated` tones → seeded demo lease (approved application + draft `LSE-DEMO-2026-001` on the townhouse, direct rows per seeder convention — no property mutation/none of the service side-effects so the demo data stays test-stable). Tests (CommitTest extends to 25): pipeline apply→approve→lease→reserved in ONE test with prefilled amounts/history/notification, approved-only, no double lease, cross-owner 404, auto-reject of rivals + notifications, end>start validation, owner list scoping, tenant list scoping, guest redirect; access matrix +5 (owner ok / tenant 403 / owner 403 on tenant / guests). Full suite 152 passed. |
| Completed slices | **Lease signing (M8) - Wave 3 slice 3 DONE**: `lease_signatures` table (`2026_09_09_000013`: lease_id FK, user_id FK, signed_at nullable-timestamp, signature_payload varchar(255), unique per lease×user) → `LeaseSignature` model + `Lease::signatures()` → `LeaseService::sendForSignature` (**owner sends only a draft**, `draft→sent`, history `lease_sent`, tenant notified via `LeaseSentForSignatureNotification`) and `LeaseService::sign` (parties only — owner-of-property or lease tenant, else 404; status must be `sent`; one signature per user; empty payload defaults to signer name; when **both** have signed the lease moves `sent→signed→active` within one transaction (history `lease_signed` per party, `lease_signed_final`, `lease_activated`) and the property moves `reserved→occupied` via PropertyService + the counterpart gets `LeaseSignedNotification`) → `LeaseController::sendForSignature`/`sign` + 3 routes (`owner.leases.send`, `owner.leases.sign`, `tenant.leases.sign`) → `Owner/Leases/Index` sends the draft, SignPad-inline for the owner, and signature chips (who signed + when); `Tenant/Leases` review-and-sign pad + signature chips; shared `LeaseSignPad` component (optional signature text, defaults server-side to the user name). Tests (CommitTest extends to 36): send draft→sent+history+tenant notified, send only from draft, cross-owner send 404, single signature keeps `sent` + payload recorded + page exposes signatures, payload defaults to name, non-tenant 404 / non-owner 404, cannot sign before `sent`, no double signatures, both-sign→`active`+property `occupied`+full history+counterpart notified, guest redirects; access matrix +3 forbidden/gated rows. Full suite 163 passed. |
| Completed slices | **Lease renew (M8) - Wave 3 slice 4 DONE**: `2026_09_09_000014` adds `renewed_from_id` (nullable self-FK to leases, nullOnDelete + index) → `Lease::renewedFrom()`/`renewals()` + fillable → `LeaseService::renew` (**owner-only via `findOwnedByOwner`, only an `active` lease, copies property/tenant/rent/deposit/payment_terms/clause_version with `application_id=null`, default `start_date` = old `end_date`, default `end_date` = start + 1y, `end > start` validated, new lease `draft` + `renewed_from_id` link + `lease_renewal` history, `lease_renewed` history row on the source, one in-flight renewal per lease guard**) → `LeaseController::renew` + route `owner.leases.renew` → `Owner/Leases/Index` renew form (inline date pickers via `useForm`, open-per-lease) + renewal-linkage chips (Renewal of / Renewed by) + `Tenant/Leases` linkage chips + renewed prose; in `sign()` both-sign activation now also **marks the source lease `renewed`** (history `lease_renewed` + `superseded_by` details) and skips the occupancy flip when the property is already `occupied` → seeded demo chain (Kumalo property occupied + `LSE-DEMO-2026-003` active + `LSE-DEMO-2026-002` draft renewal, direct rows). Tests (CommitTest extends to 44): renew copies terms, renewed dates validated + defaults, only-active can renew, cross-owner 404, one-in-flight guard, renewed-lease cannot renew, signing a renewal activates it + marks source renewed, tenant lease page exposes linkage (asserted by lease_no, not index order), guest redirect; access matrix +2 (tenant 403, guest redirect). Full suite 171 passed. |
| Completed slices | **Document store (M20-lite) - Wave 3 slice 5 DONE** (**WAVE 3 COMPLETE**): `documents` table (`2026_09_09_000015`: lease_id nullable FK cascadeOnDelete, type default `lease_agreement`, name, content longText, mime default `text/plain`, size, version default 1, visibility default `private`, indexes (lease_id, version) + type) → `Document` model + `Lease::document()` hasOne → `DocumentService` (`renderAgreementText`: Dzimba agreement snapshot with parties, property, term, financial terms, every signature payload + signed_at, clause version; `storeLeaseAgreement`: create or bump `version` in place, keeps one row per lease mirroring FR-03; `authorizeFor`: admin/superuser OR lease property owner OR lease tenant, everyone else 404; `listForOwner`/`listForTenant` scoped selects) → hooked into `LeaseService::sign()` so every activated lease (fresh or renewal) stores its agreement → `DocumentController` + 4 routes (`owner.documents.index`, `tenant.documents.index` in role groups; `documents.show`/`documents.download` in the app-wide auth group, download streams `text/plain` with `Content-Disposition: attachment` + versioned filename) → `Owner/Documents/Index` + `Tenant/Documents` (folder cards) + shared `Documents/Show` (residency header, Download, `<pre>` snapshot) + FolderOpen nav entries + "View agreement / Download" links on every lease card (owner + tenant) → seeded demo document for active `LSE-DEMO-2026-003` rendered through the service with an existence guard (re-seed never revisons). Tests (CommitTest extends to 54): both-signs stores agreement with signatures + version 1 + size; renewal signing stores its own agreement; owner+tenant view; tenant download (charset, attachment header, body); cross-tenant + cross-owner 404 (view + download); admin access; scoped indexes; version bump; guest redirects. Access matrix extends to 52 rows. **Full suite 189 passed / 1020 assertions.** |
| Completed slices | **Configuration engine (M24) - Wave 4 slice 1 DONE**: `system_configurations` + `configuration_audits` tables (`2026_09_09_000017`) → `SystemConfiguration`/`ConfigurationAudit`/`SubscriptionFeature` models + `SubscriptionPlan::features()` grant pivot (explicit `plan_id`/`feature_id` keys) → `ConfigurationService` (cached `dzimba.config.<key>`, typed casts incl. decimal-as-string, `set` audits old/new/by/at/reason, high/critical risk requires `approved_by` — admin records self as approver in the UI) → `ConfigSeeder` wire-up (20 billing defaults, 14-feature catalogue, 29 plan grants: Free→listing/messaging/viewing/application, Basic +featured, Professional +analytics/financial/advanced-search, Business +rent-collection/maintenance/multi-user/dedicated-support) → `AdminConfigController` index/update + 2 admin routes + `AdminPlansController::features` grant toggle → AdminLayout "Configuration Centre" nav → `Admin/Configuration/Index.jsx` (grouped ConfigRow in-page editors w/ per-key risk tones on `StatusBadge`, FeatureToggler per plan, last-10 audit trail) → billing engines conformed (grace, proration rule/credit, cycle days, renewal reminders, invoice/receipt numbering + padding, effective-limit where null = unlimited, over-limit message, suspension behaviour keep/hide listings; `PropertyService::changeStatus` quota gate counts the listing returning to available) → new `ConfigurationTest` suite (13 tests: seeded baseline, config-change→grace lifecycle, audit row + cache invalidate, high-risk approval gate, numbering + padding from config, over-limit message from config, suspension keep-vs-hide in marketplace, seeded per-catalogue grants, centre update applies a rule, unknown/locked key blocked) + access matrix extended (admin-ok / owner-tenant-403 / guest-redirect, plan-feature toggle admin-only; relation FK `plan_id` pinned on `SubscriptionPlan::subscriptions()`/`Subscription::plan()`). Full suite 223 passed. |
| Completed slices | **Plans/subscribe config-driven (M12) - Wave 4 slice 2 DONE**: the subscription engine now consumes every commercial rule from `ConfigurationService` + DB-backed plans end-to-end. Removed the `Subscription::GRACE_DAYS` code constant (AC-04/AC-05) — `graceUntil()` only ever resolves the stored `grace_until` (service always writes `ends_at + grace_period_days` config on entering grace) and falls back to `ends_at` instead of a magic number. `upgrade()` now branches on `subscriptions.proration.mode` (previously hard-coded `credit_new_invoice`): **`credit_new_invoice`** (default) keeps the prorated-credit path, **`charge_difference`** charges `new − current` with zero credit, **`apply_at_renewal`** defers via a new `pending_upgrade_to` details key (`Subscription::pendingUpgradeTo()` + `upgrade_pending` history event) with no invoice/plan change until the boundary. `applyCycleEnd()` generalised to settle **either** deferred plan change via `pendingPlanTarget()` (upgrade or downgrade, `cycle_end`/`grace_end` event), resolving the new plan's billing cycle and clearing both pending keys (`switchEqual`/`downgrade`/`upgrade` also reconcile them). Owner subscription page surfaces the live `proration_mode` from config so the copy ("instantly at a pro-rated rate" vs "at the end of your current cycle") never misleads. Tests: 3 new MonetiseTest cases (`charge_difference` charges $10.00 diff Basic→Professional mid-cycle with credit 0.00; `apply_at_renewal` defers with no extra invoice then settles to Professional at cycle_end; default proration unchanged $12.50) + 1 new ConfigurationTest case (change proration config → old invoice amount/invoice_no untouched, subsequent upgrade uses the new mode). Full suite 226 passed. |
| Completed slices | **Rent schedule (M9) - Wave 4 slice 3 DONE**: `rent_schedules` + `rent_invoices` tables (`2026_09_09_000018`: schedule = lease_id unique FK + start/end/rent_amount DECIMAL(12,2)/payment_terms JSON; invoice = property/tenant/lease/schedule FKs cascade, period_start/period_end, amount DECIMAL(12,2), status ENUM(draft,due,paid,overdue,cancelled,refunded) default draft, invoice_no unique, reminded_at, indexes) → `RentSchedule` (lease()/invoices()/outstandingInvoices()) + `RentInvoice` (explicit state machine STATUSES/TRANSITIONS/canTransitionTo + property/tenant/lease/schedule relations) → `RentService` (final, `ConfigurationService`-injected, every rule reads config — nothing hard-coded): `generateFor` (active-lease-only + idempotent, never double-bills; period builder splits the term into contiguous calendar months with first/last clamped to term boundaries — Jan 15→Mar 10 = 3 invoices; whole-term coverage, zero overlap), `runInvoiceLifecycle`/`advance` (draft→due when the configured due date arrives — `invoices.timing` billing_date=period start / immediate=created_at; due→overdue once period_end + `invoices.overdue_days` passes; one-time due reminder within `invoices.reminder_lead_days` of due date via `RentInvoiceDueNotification`, recorded in `reminded_at`, never repeated, overdue skipped), `nextInvoiceNo` (`RNT-YYYY-NNNN` from `numbering.rent_invoice.prefix` + shared padding/start/year-reset), `schedulesForOwner`/`ownerSummary`/`invoicesForTenant`/`tenantSummary` (all scoped server-side — owner via property whereHas, tenant via tenant_id; money sums from the ledger, never client arithmetic) → `rent:process` command (`RentInvoiceProcessCommand`, auto-discovered) → hooked into `LeaseService::sign()` so every activated lease generates its schedule + invoices at signing → `RentController` + 2 routes (`owner.rent.index` "Rent & Income": schedules grouped by property with invoice tables + Invoiced/Due/Overdue/Paid StatCards; `tenant.rent.index` "My Rent": invoices grouped by lease + Due/Overdue/Paid StatCards) → Receipt nav entries for both roles → `StatusBadge` due/overdue/refunded tones → `Tenant/Leases` stale "later phase" copy replaced with a live rent-invoices link → seeded demo: active `LSE-DEMO-2026-003` gets its full schedule (13 invoices — past overdue, live due, future draft, invoice_no RNT-2026-0001; guarded so re-seeding never re-bills) then the lifecycle runs in the seeder. New config defaults: `numbering.rent_invoice.prefix` (RNT), `invoices.timing` (billing_date), `invoices.reminder_lead_days` (3), `invoices.overdue_days` (0) — the `invoices` group auto-appears in the admin Configuration Centre; `ConfigSeeder` is key-based + updateOrCreate so defaults are idempotent. Tests: 6 new MonetiseTest cases (full-term 12 invoices with exact amounts + `RNT-2026-` numbering; partial start/end months exact + contiguous; generation idempotent + inactive leases generate nothing; lifecycle draft→due→overdue with exactly one reminder + reminded_at set + `Notification::assertSentToTimes` pinned; signing hook activates a lease and generates its schedule; tenant/owner queries scoped) + access matrix +5 (owner/tenant rent pages ok, opposite-role 403, guest redirect). **Full suite 237 passed / 1255 assertions.** |
| Completed slices | **Payment (M9) - Wave 4 slice 4 DONE**: `payments` table (`2026_09_09_000019`: invoice_id FK→rent_invoices cascade, paid_by/received_by FKs→**auth_users** cascade/nullOnDelete, amount DECIMAL(12,2), method ENUM(cash,bank,mobile,online), reference ≤255 nullable, pop_path nullable, receipt_no ≤40 unique nullable, status ENUM(pending,settled,rejected,refunded) default pending, paid_at nullable, indexes invoice_id/paid_by/(status,created_at)) → `Payment` explicit state machine (pending→[settled,rejected], settled→[refunded], rejected/refunded terminal) + invoice()/paidBy()/receivedBy() + `RentInvoice::payment()` hasOne latest + `payments()` + `ReceiptIssuedNotification` → `PaymentService` (final, `ConfigurationService`-injected — zero commercial constants): `recordPayment` (tenant-scoped, foreign invoice→404; due/overdue-only; **idempotent per invoice** — a pending/settled payment blocks re-pay (NFR-02); **exact-amount only** via `normalise()`; `enabledMethods()` from `payments.methods`; bank/mobile demand a POP when `payments.pop.approval_required`; auto-pending when POP raised for bank/mobile **or** `cents(amount) ≥ cents(payments.approval.threshold)` (500.00), else settles instantly through the state machine), `approve`/`reject` (approve→settled + unique receipt (`RCT-xxxxxxxx` from `numbering.receipt.prefix`+`.length`, uniqueness loop) + invoice→`paid` + tenant notified; reject keeps the invoice owing and re-pay allowed), `pendingForAdmin`/`recentForAdmin`, `renderReceiptText` (streamed `DZIMBA — RENT RECEIPT` text) → `PaymentController` + 5 routes (`tenant.rent.pay` POST (redirect to `tenant.rent.index` + flash), `tenant.rent.receipt` download (payer or Admin/Superuser else 404), `admin.rent.payments.index/approve/reject`) → `Tenant/Rent` rewritten (per-invoice PayForm: amount/method/reference + POP file picker for bank/mobile when `pop_required`, reads live payment `rules`; due/overdue→pay, settled→receipt download link, pending→"waiting for confirmation"; Due-now/Overdue/Paid/Receipts StatCards) → `Admin/Payments/Index` approval queue (pending table w/ Approve/Reject + recent settled/rejected trail) + AdminLayout "Payment Approvals" nav under Subscriptions & Billing + StatusBadge `settled` tone → demo pending payment on the live due invoice (`RNT-2026-0011`, cash, $1,000.00 ≥ threshold ⇒ staff see it in the queue; re-seed guarded by Payment existence). Tests: 8 new MonetiseTest cases (instant settle + receipt notification, exact-amount mismatch, double-pay blocked, high-value waits → staff approve settles + notifies, reject → re-pay allowed, bank-POP required, receipt numbers unique, cross-tenant 404, settling removes the invoice from arrears totals) + access matrix +9 rows (approvals admin-ok/tenant-403/owner-403/guest-redirect, tenant pay-own→redirect + download issued receipt, foreign receipt 404, cross-tenant pay 404, guest pay/receipt redirects). **Full suite 253 passed / 1300 assertions**, build clean, `route:list` clean, `migrate:fresh --seed` verified (1 pending demo payment + rules exposed). |
| Completed slices | **Property Marketplace (M3-expanded) - Discovery & Trust batch B1-B6 DONE (Sept 2026)**: the `docs/modules/# Property Marketplace.md` spec got built out end-to-end. **Search**: `PropertySearchService` adds free-text `q`, `verified`/`furnished`/`availability`/`amenities`(AND)/geo `lat+lng+radius_km` filters and `price_per_m2` sort on top of the existing type/zone/suburb/price/beds/baths set; `HomeController` handles `search.suggestions` JSON autocomplete; map marker styles (`dz-map-pin`/`dz-map-pop`) in `resources/css/app.css`. **Detail page**: `Marketplace/Show.jsx` rebuilt (gallery, leaflet map, financial breakdown rent+deposit+move-in, owner preview + Enquire/Book-viewing/Apply, favourite, safety notice + report modal, similar/recommended/recently-viewed rails, mobile sticky CTA). **Trust**: `reports` table + `Report` explicit state machine (`open↔under_review↔escalated`, `resolved` terminal, `dismissed` reopenable) + `ReportService` (public guest-friendly create, reporter list, admin queue + stats + note + optional `hide_listing` takedown with history) + `tenant.reports.index` + `admin.marketplace.reports.{index,transition}` pages; fix: `ReportService::create` no longer crashes without a `priority`. **Saved searches**: `saved_searches` + `SavedSearchService` (per-tenant CRUD 404-scoped, live `match_count`, `matches()` + `alertable()`) + `tenant.saved-searches.*` routes + `Tenant/SavedSearches` page; match/price-drop/availability alerts via `MarketplaceAlertService` (24 h dedupe). **Lifecycle**: `listings.validity_days/validity_reminders/auto_expire/renew_grace_days` config → `ListingLifecycleService` (expireDue/dueSoon/renew quota+grace guarded) + `marketplace:housekeeping` command + `ListingExpiryReminderNotification`; `PropertyService` stamps `expires_at` on publish. **Analytics**: `MarketplaceAnalyticsService` → `Owner/Analytics` (owner-scoped, per-property views/favourites/enquiries/applications, 30-day trend, top property) + `Admin/Marketplace/Analytics` (platform totals, averages, trend, most-viewed, top areas). Fixes: `PropertyHistory` gained `note` column (`2026_09_09_000025`) + fillable (status-change notes were silently dropped); admin pages use the real `admin.marketplace.*` route names; `Tenant/Leases` orphaned `marketplace.show` link → `property.show`. Tests: `ListingAndDiscoverTest` 47 (keyword/radius/price-per-m²/suggestions, saved-search CRUD + match counts + cross-tenant 404, lifecycle expiry/reminder/renew + grace guard), new `TrustGovernTest` 11 (public report, validation, own-reports isolation, queue stats, resolve-with-note, state machine, hide-listing takedown, analytics scoping), access matrix 95 rows. **Full suite 295 passed / 1457 assertions; build clean; `migrate:fresh --seed` verified.** |
| Completed slices | **Arrears & income (M9) - Wave 4 slice 5 DONE**: `rent_invoices.late_fee` DECIMAL(12,2) default 0 (`2026_09_09_000026`) + `late_fees.*` config keys (`enabled` bool, `type` fixed|percent, `value`, `cap`, `period_days` — zero numbers in code) → `RentService::runInvoiceLifecycle($notify)` + `accrueLateFees()` (deterministic, idempotent, recompute-from-config: per-period charge = fixed or percent-of-invoice, period count = overdue days ÷ `period_days` + 1, hard `cap`) + `lateFeeFor()`; the due→overdue transition fires `RentInvoiceOverdueNotification` once per invoice to tenant + property owner (role-aware deep link; seeded demo history stays silent via `runInvoiceLifecycle(false)` in the seeder — fixed a notification-order regression in `EngageTest`) → arrear statements `tenantStatement`/`ownerStatement` (per-invoice rows: invoice_no, property, lease_no, period, amount, late fee, days overdue + server-side totals; AC-04 exactness) surfaced on both rent pages (owner arrear section + late-fee column, tenant "My Rent" statement) → new `FinancialSummaryService::ownerIndex()` (monthly income from settled `paid_at`, 6-month income trend, rent due/outstanding/arrears/late-fees totals, occupancy rate + occupied/total, all owner-scoped) → `OwnerDashboardController` injects it — the Rent Due stat now reads the ledger `outstanding_total` + new Monthly Income and Occupancy KPI cards. Tests: +10 `MonetiseTest` cases (percent late fee idempotent, cap accumulation, fixed rate, disabled, overdue notification once-per-invoice to both parties, tenant statement exactness, owner statement isolation, monthly income aggregation, occupancy rate, dashboard financial props). Full suite 305 passed / 1500 assertions. |
| Completed slices | **Featured ads (M13) - Wave 4 slice 6 DONE** (**WAVE 4 COMPLETE**): `ad_packages` (code unique, name, placement_type ENUM(featured,top,homepage,premium_badge), price DECIMAL(12,2), duration_days, description, is_active) + `ad_placements` (property_id/owner_id FKs cascade, package_id nullable nullOnDelete, **amount price snapshot**, starts_at/ends_at/paused_at/paid_at nullable timestamps, status ENUM(reserved,active,paused,expired,cancelled) default reserved, credit_amount DECIMAL(12,2), admin_note ≤500, indexes status + (status,ends_at)) + `ad_events` (placement_id FK cascade, event_type ENUM(impression,click,enquiry,application), created_at only) (`2026_09_09_000027`) → `AdPlacement` explicit state machine (`OPEN_STATUSES` = reserved/active/paused) → `AdPlacementService` (final, `ConfigurationService`-injected — **zero commercial constants; every price/duration/window reads the package catalogue + `featured.*` config fallbacks**): `book` (featured.enabled gate, owner-scope, available-only, per-property slot = `featured.max_per_property` (0 disables), spend cap = `featured.max_active_per_owner`; amount = package price, `reserved` order or auto-`approve` when `featured.approval_required` is off), `approve` (reserved-only, window now→now+`duration_days` (fallback `featured.auto_expiry_days`), `paid_at` settlement — the M9-style approval gateway, property `featured=true`, `AdPlacementActivatedNotification`), `cancel` (reserved = full credit; active/paused = **prorated unused window, integer cents via `intdiv`** → `credit_amount` + `AdPlacementCancelledNotification`), `pause`/`resume` (moderation freeze; resume extends `ends_at` by the frozen span), `expireDue` (`ads:expire` cmd, daily schedule in `routes/console.php`, NFR-01), `trackForProperty`/`recordImpressionsFor`/`statsFor` (impressions from the marketplace featured rail, clicks from public detail, enquiry/application events from `EnquiryService`/`ApplicationService` — NFR-03 no PII) → `AdPlacementController` + 7 routes (`owner.advertising.{index,store}` page: packages grid + book-your-available-listing form + placements with per-event stats + StatCards; `admin.advertising.{index,approve,cancel,pause,resume}`: approval queue, live moderation with inline note, prorated-credit history) + admin/owner nav entries (Megaphone) + StatusBadge `paused` tone → `AdvertisingSeeder` (4 demo packages $10/$25/$40/$15 × 30/14/7/30d; two demo active placements on the already-`featured` Borrowdale house + Bulawayo townhouse, one `reserved` top-placement on the 2 Bedroom Flat — direct rows, test-stable) + 6 new `featured.*` config rows (enabled / approval_required / max_per_property / max_active_per_owner / default_price / auto_expiry_days, seeded by `ConfigSeeder` from `ConfigurationService::defaults()`). Tests: new `AdsTest` (23: booking snapshot + every guard, approval window/paid_at/featured/notify, only-reserved approval, full vs prorated-credit exactness (travel-based), terminal immutability, pause/resume extension, expiry sweep + rebook, 4-event attribution stats, owner/admin HTTP flows, seeding) + access matrix +7 (owner ok / tenant-tenant 403 / admin ok / guest redirects). Full suite 335 passed / 1603 assertions. |
| Completed slices | **Maintenance report & triage (M10) - Wave 5 slice 1 DONE**: `maintenance_requests` (request_no ≤20 unique `MR-YYYY-NNNNN`, property_id/tenant_id FKs cascade, category ENUM(plumbing,electrical,appliance,structural,pest,safety,other), priority ENUM(low,medium,high,emergency), title ≤120, description text, status ENUM(reported,assigned,in_progress,completed,closed,declined) default reported, sla_due_at/escalated_at nullable timestamps, index (status,sla_due_at)) + `maintenance_actions` timeline (request_id FK cascade, actor_id nullable→auth_users nullOnDelete, action ≤40, notes ≤500, created_at only) (`2026_09_09_000028`) → `MaintenanceRequest` (STATUSES/TRANSITIONS/TERMINAL/PRIORITIES) + `MaintenanceAction` (FK pinned `request_id` on the hasMany/belongsTo — same Eloquent trap as ad_events) → `maintenance.*` config group (request_no.padding=5, categories JSON, escalation_enabled, sla.{low=168,medium=96,high=48,emergency=24}h) → `MaintenanceService` (final, `ConfigurationService`-injected — **no SLA hours/categories in code**): `create` (reportable = actively-leased properties only — foreign lease = 404; category/priority from config; `sla_due_at` = now + priority hours from config; unique MR number from max(id)+1; `reported` timeline action; owner notified `NewMaintenanceRequestNotification`, emergency additionally pages all Admin/Superuser via `EmergencyMaintenanceNotification` — AC-03), `listForTenant`/`listForOwner` (owner scoped via property whereHas), `escalateDue` (status reported + past `sla_due_at` + `escalated_at` guard = **idempotent once-per-request**; sets `escalated_at`, logs `escalated`, pages staff `MaintenanceSlaBreachedNotification`; master-switch `maintenance.escalation_enabled`), `listEscalationsForAdmin` + `acknowledge` (reported+escalated-only, clears `escalated_at`, logs `acknowledged` owner, case continues in the owner queue — FR-07/NFR-01) → `MaintenanceEscalateCommand` (`maintenance:escalate` daily 06:00 in `routes/console.php`) → `MaintenanceController` + 5 routes (`tenant.maintenance.{index,store}`, `owner.maintenance.index`, `admin.maintenance.escalations.{index,ack}`) → `Tenant/Maintenance` (report form on leased homes + request tracker w/ SLA countdown + emergency banner), `Owner/Maintenance/Index` (repair queue sorted breach-then-priority, SLA chips, escalated badges), `Admin/Maintenance/Escalations` (staff queue + Take ownership), Wrench nav entries (MainLayout owner+tenant, AdminLayout), StatusBadge reported/assigned/in_progress tones → `MaintenanceSeeder` demo rows MR-2026-00001 (reported plumbing) + MR-2026-00002 (emergency safety, escalated — silent direct rows) → new `OperateTest` suite (15 tests: report creates MR + SLA + timeline, emergency pages owner+staff, active-lease-only 404, category/priority validation, config-driven SLA hours, escalate-once + idempotent + staff notified, non-reported skipped, master switch, admin queue + ack clears, ack-guard, owner row isolation, tenant leased-properties page, seeded emergency badged, state machine spec) + access matrix +10 rows → full suite 360 passed / 1708 assertions, build clean, dev DB seeded. |
| Completed slices | **Maintenance assignment (M11) + contractor registry - Wave 5 slice 2 DONE**: `contractors` (user_id nullable FK→auth_users nullOnDelete, business_name ≤120, contact ≤120, service_area JSON, status ENUM(unverified,vetting,verified,suspended) default unverified, rating_avg DECIMAL(3,2) default 0, jobs_completed unsignedInt default 0, verified_at nullable, index (user_id,status)) + `contractor_trades` (contractor_id FK cascade, trade ≤60, rate DECIMAL(10,2) nullable) (`2026_09_09_000029`); `maintenance_requests` gains `approved_quote` DECIMAL(12,2) + `assigned_contractor_id` FK nullOnDelete (index) → `Contractor` model (STATUSES/TRANSITIONS: unverified→vetting→verified↔suspended, terminal checked via `canTransitionTo`; relations user/trades/assignments) + `ContractorTrade` + `MaintenanceRequest::contractor()` → `ContractorService`: `listForAdmin` (paginate 12, withCount assignments), `verifiedForAssign` (verified only, rating/jobs desc — the owner dropdown), `register` (always lands `vetting`, trades validated/normalised — admin always confirms before verified), `setStatus` (state-machine guarded, stamps `verified_at` on verified, clears it when leaving verified) → **`MaintenanceService::assignToContractor`** (404 unless the request's property belongs to the caller — row-level owner isolation; **status must be `reported` → double-assign blocked** (NFR-02); quote `required|numeric|min:0` stored `number_format(...,2)` DECIMAL(12,2) server-side; **only `verified` contractor assignable** (AC-01/AC-03 — vetting/suspended rejected); sets status `assigned` + quote + contractor, clears `escalated_at` (re-opening the case), logs `assigned` timeline action w/ actor + "quote — business" notes, notifies the linked contractor user `MaintenanceAssignedNotification` (job brief w/ quote, Module 10 AC-02)) + `listForContractor` (scoped to own profile, eager-loads property.owner, empty when no profile) → `ContractorController` (`adminIndex`, `adminStore`, `adminStatus`, `contractorIndex` the contractor job desk) + routes: `owner.maintenance.assign` POST whereNumber, contractor group `role:Contractor` GET `/contractor/maintenance`, admin `admin.contractors.{index,store,status}` → `LoginController::landingFor` Contractor branch (contractor signs in straight to the job desk) → demo `contractor@dzimba.local` (Contractor role, verified, linked to Bulawayo Plumbing Co.), `ContractorSeeder` (Bulawayo Plumbing Co. verified+linked, Mura Building Services verified standalone, CleanFlow Pest Solutions vetting — **runs before MaintenanceSeeder** which now seeds MR-2026-00001 as **assigned** to Bulawayo Plumbing Co. @ $85.00 + `assigned` action) → frontend: `Admin/Contractors/Index.jsx` (register form w/ trade rows + registry cards + transition buttons), `Contractor/Maintenance/Index.jsx` (my-jobs brief cards w/ quote chip), Owner `Maintenance/Index.jsx` `AssignForm` (verified-contractor select + quote + assign on `reported` cards; assigned cards show contractor + quote chip; stat card "Awaiting assignment"), MainLayout contractor section "My Jobs", AdminLayout "Contractor Registry" nav, StatusBadge `unverified`/`vetting`/`verified` tones. Tests: OperateTest +9 (register→vetting→verify stamp; assign happy path w/ quote normalisation + owner timeline + cleared escalation + contractor notified; only-verified assignable; double-assign blocked; cross-owner 404; quote required/negative; contractor jobs page own-briefs-only + stranger profile-empty; registry state machine vetting→suspended blocked; suspended contractor → no new assignments + verified_at cleared) — OperateTest 24, access matrix +9 (owner assign ok / tenant 403 / guest redirect; admin registry ok; owner+tenant 403; guest redirect; contractor jobs ok / owner+tenant 403 / guest redirect). Full suite 378 passed. |
| Completed slices | **Track & close out (M10/M11) - Wave 5 slice 3 DONE**: `maintenance_requests` + `tenant_confirmed_at` + `resolved_at` nullable timestamps (`2026_09_09_000030`) → `MaintenanceService` lifecycle: **`start`** (assigned→in_progress, only the assigned contractor — profile behind login, 404 otherwise, `started` timeline action, owner notified `MaintenanceStartedNotification`), **`complete`** (in_progress→completed, required summary ≤500 → `completed` action w/ notes; pages owner + tenant `MaintenanceWorkCompletedNotification`), **`confirm`** (tenant-only via request ownership; requires `completed`; one-shot gate stamps `tenant_confirmed_at`, `tenant_confirmed` action, owner notified `MaintenanceTenantConfirmedNotification`), **`close`** (owner-only via property ownership; requires `completed` + tenant confirmation — **tenant confirm gates close**; sets `closed` + `resolved_at`, retains `approved_quote` → feeds maintenance spend (AC-04/NFR-03); `closed` action w/ optional inspection note; tenant + contractor user notified `MaintenanceClosedNotification`) → routes `owner.maintenance.close`, `tenant.maintenance.confirm`, `contractor.maintenance.{start,complete}` (whereNumber + descriptions) → frontend: contractor desk Start button + complete-with-note inline form + "Fix confirmed" chip; tenant Confirm-fix button + confirmed chip; owner queue includes `completed` cards w/ tenant-confirmed chip + Inspect & close form → StatusBadge already had the tones; no seeder change (demo drives the flow in-browser). Tests: OperateTest +7 (start→complete full path w/ notes + owner/tenant notified; stranger contractor 404; lifecycle gate matrix — start-non-assigned, complete-non-in-progress, complete-without-notes; tenant one-shot confirm → owner close → resolved_at + quote retained + tenant/contractor notified; confirm-gates-close validation; tenant own-request 404 + confirm-uncompleted rejected; cross-owner close 404) — OperateTest 31; access matrix +9 (contractor start/complete ok / owner+tenant 403 / guest redirect; tenant confirm ok / owner+contractor 403 / guest redirect; owner close ok / tenant+contractor 403 / guest redirect). Full suite 394 passed. |
| Completed slices | **Contractor ratings (M11) - Wave 5 slice 4 DONE** (**WAVE 5 COMPLETE**): `contractor_ratings` (`2026_09_09_000031`: request_id FK→maintenance_requests **unique** (one rating per job), contractor_id FK cascade, owner_id nullable FK→auth_users nullOnDelete, rating unsignedTinyInteger 1-5, note ≤500 nullable, index contractor_id) → `ContractorRating` model + `Contractor::ratings()` + `MaintenanceRequest::rating()` hasOne → **`ContractorService::rate`** (owner-only via property ownership 404; **status must be `closed`** — ratings reflect completed jobs only (AC-02), a declined/no-contractor request is rejected; **one rating per request** — schema unique enforces the second-post guard; rating required `1-5` + note ≤500; creates the row, then **recomputes `rating_avg` as a jobs-weighted average `(rating_avg × jobs_completed + score) ÷ (jobs_completed + 1)` rounded to 2dp server-side** + increments `jobs_completed`; logs a `rated` timeline action `5/5 — note`; notifies the linked contractor user `ContractorRatedNotification` → all server-side, never client arithmetic) → `MaintenanceController::ownerRate` + route `owner.maintenance.rate` (POST, whereNumber + description) → `listForOwner` eager-loads `rating` → frontend: owner queue gains a "Recently closed" section — closed cards show the contractor + quote and a star-pick **RateForm** (1-5 + feedback note) or the "You rated n/5" chip once done; assign dropdown shows each contractor's `★ rating_avg (N jobs)`; contractor desk header shows their average + completed jobs. Demo: the seeded MR-2026-00001 (assigned, Bulawayo Plumbing Co.) drives start→complete→confirm→close→rate in the browser; no seeder change. Tests: OperateTest +6 (rate-after-close full-path with rating row + `rated` action + aggregate recompute 4.80×12+4→4.74/13 + contractor notified; rating-requires-closed — in_progress/completed-unclosed/declined all rejected; one-per-request second-post rejected; recompute correctness — established history 3.00/5 + 4 → 3.17/6 and brand-new contractor 0/0 + 5 → 5.00/1; validation payloads (empty/0/6/`good`/501-char note); cross-owner 404) — OperateTest 37; access matrix +3 (owner rate ok / tenant+contractor 403 / guest redirect). Full suite 403 passed. |
| **NEXT UP** | **Verification (M14) - Wave 6 slice 1**: owner submits a verification case with supporting evidence → admin reviews → badge (`verified` on the owner/property) with the latest decision reflected; evidence is never public; revoking a badge demotes the badge on the marketplace. Wave-tests: badge reflects latest decision; evidence never public; revoke demotes. |
| In flight | (none) — **WAVE 5 IS COMPLETE** (Maintenance M10 + Contractors M11: report & triage, assignment + registry, track & close out, contractor ratings). |
| Blocked by | (none) |
| Last updated | 2026-09-10 (Wave 5 slice 4 DONE — Contractor ratings; **Wave 5 COMPLETE**; 4 vertical slices remain: Wave 6 ×4) |

Wave 2 (Engage) is COMPLETE; Wave 3 (Commit) is COMPLETE (slices 1–5: Applications, Lease create, Lease signing, Lease renew, Document store); **Wave 4 (Monetise) is COMPLETE** (slices 1–6: Configuration engine M24, Plans/subscribe config-driven M12, Rent schedule, Payment, Arrears & income M9, and **Featured ads M13**); the **Property Marketplace batch (Discovery & Trust, B1-B6) is COMPLETE** (search engine, detail experience, saved searches + alerts, listing lifecycle, reports/moderation, owner + admin analytics — see the `# Property Marketplace.md` doc §63 and the tracker row above). **Wave 5 (Operate — M10 + M11) is COMPLETE**: slices 1–4 (Maintenance report & triage, assignment + contractor registry, track & close out, and contractor ratings) are all DONE. **Next: Wave 6 — Trust & Govern (M14 + M19 + M18 + M17), starting with Verification (M14).**

## 2. Definitive Build Order of the Major Modules

Every major module has a rank. Lower rank = built earlier. The **Wave** column shows what it ships with; it never ships alone when it is part of a coupled group.

| Rank | Module | Wave | Must be built together with | Why this order |
|---|---|---|---|---|
| 1 | **User & Account (M1)** | Wave 0 (DONE) | Roles, middleware, layouts, navigation | Every other module needs an authenticated, correctly role-gated user. |
| 2 | **Property Management (M2)** | Wave 1 | **Marketplace (M3), Favourites (M4)** | The owner cannot add value until listings exist; the tenant cannot discover without the marketplace; favourites are meaningless without listings. **This is the content engine. Build it first.** |
| 3 | **Marketplace / Search (M3)** | Wave 1 | **M2, M4** | Consumes M2 only; without it M2 has no outward face. |
| 4 | **Favourites & Saved Searches (M4)** | Wave 1 | **M2, M3** | Smallest module; depends on both. Saved-search alerts can split to Wave 5 (needs Notifications). |
| 5 | **Enquiries & Communication (M5)** | Wave 2 | **Viewings (M6)** | Both are "tenant initiates → owner responds" conversational flows sharing threads, statuses and notifications. Building the pattern once saves a full module of rework. |
| 6 | **Property Viewings (M6)** | Wave 2 | **M5** | Scheduling requests ride on the communication/notification pattern established by M5. |
| 7 | **Rental Applications (M7)** | Wave 3 | **Leases (M8), Documents-lite (M20)** | Application **approval is meaningless until it becomes a lease**. The pipeline tenant-applies → owner-approves → lease-signs → property-occupied is one unbroken vertical. |
| 8 | **Lease & Agreements (M8)** | Wave 3 | **M7, M20** | Cannot exist without an approved application; produces the documents that M20 must store. |
| 9 | **Documents (M20)** | Wave 3 | **M7, M8 (start), reused everywhere after** | Only the lease-signing store is built in Wave 3; the full repository grows with later waves. |
| 10 | **System Configuration (M24)** | Wave 4 | **M12, M9, M13** | **The configuration-driven directive is a build-order constraint: no billing/commercial rule may be written before `ConfigurationService` + Configuration Centre exists.** Grace, proration mode, suspension behaviour, invoice/receipt numbering, cycle days, renewal reminders, payment/POP/approval, late fees and featured pricing are data, not code. |
| 11 | **Subscriptions (M12)** | Wave 4 | **M24, M9, M13** | **All monetise through one configuration-driven billing engine and one shared payment/ledger pattern.** Quota enforcement also hooks the M2 publish action. |
| 12 | **Rent & Payments (M9)** | Wave 4 | **M24, M12, M13** | Provides the gateway + ledger that subscriptions and ad placements reuse; every rule reads config. |
| 13 | **Featured & Advertising (M13)** | Wave 4 | **M24, M9, M12** | Buys placements that M3 renders; pays through the M9 gateway; pricing/duration from config. |
| 14 | **Maintenance (M10)** | Wave 5 | **Contractors (M11)** | A maintenance job that cannot be assigned to a verified tradesperson is half a module. Ratings only make sense against completed jobs. |
| 15 | **Contractors (M11)** | Wave 5 | **M10** | Only becomes useful once there are jobs to assign. |
| 16 | **Verification (M14)** | Wave 6 | **Disputes (M19), Admin completion (M18), Reports (M17)** | Verification badges and dispute resolution are both admin-trust functions sharing the evidence/document store and the admin queue; reports make them measurable. |
| 17 | **Complaints & Disputes (M19)** | Wave 6 | **M14, M18** | Needs the verification state (badge holds) and evidence from earlier waves. |
| 18 | **Admin / System Management (M18)** | Wave 6 | **M14, M19, M17, M24** | The console is completed last because by then every admin surface (incl. the Configuration Centre and approval queues) exists to be wired in. |
| 19 | **Reports & Analytics (M17)** | Wave 6 | **M14, M18, M24** | No meaningful report can be validated before ledgers and cases exist; figures derive from config-driven ledgers. |
| - | **Notifications (M15)** | Cross-cutting | Starts in-app in Wave 1; email/SMS/WhatsApp grow each wave | Every wave raises events. Start the bell in Wave 1, add channels when each workflow needs them (timing/templates become config in Module 24). |
| - | **Landlord Dashboard (M16)** | Cross-cutting | Progressively enriched every wave | Starts with property/application cards (DONE); gains rent, maintenance and analytics cards as those modules land. |

### What this means for "start development"

The **first major modules to be developed** are **Property Management (M2) + Marketplace (M3) + Favourites (M4) in Wave 1** - because they are the only wave that produces a complete, testable business loop (owner lists → tenant finds → tenant saves) with zero dependence on unfinished work. Nothing else can start before it, and nothing in it depends on later modules.

## 3. Party Impact & Relationship Map

For every major module the three parties have a different role: **Driver** (the one the feature exists for), **Responder** (opposite side of the transaction), **Governor/Monitor** (admin oversight). "Impact" = the party whose data, dashboard or process changes.

| Module | Impact on Admin | Impact on Owner | Impact on Tenant | Has Driver→Responder pair |
|---|---|---|---|---|
| M1 Users & Accounts | Driver (creates/audits) | uses (profile/security) | uses (profile/security) | n/a (platform) |
| M2 Properties | Governor (moderate/verify) | **Driver (list & manage)** | views (cards, detail) | Owner → Tenant |
| M3 Marketplace | Governor (config/moderation) | benefits (visibility) | **Driver (search & filter)** | Tenant ← Owner |
| M4 Favourites | - | benefits (save signals) | **Driver (save/list)** | Tenant only |
| M5 Enquiries | Monitor (audit/abuse) | Responder (reply) | **Driver (ask)** | **Tenant → Owner** |
| M6 Viewings | Monitor (SLA/abuse) | Responder (schedule/accept) | **Driver (request)** | **Tenant → Owner** |
| M7 Applications | Governor (oversight) | **Driver (review/approve)** | Responder (apply/track) | Tenant → **Owner** |
| M8 Leases | Governor (templates/oversight) | **Driver (create/sign)** | Responder (review/sign) | Owner → Tenant |
| M9 Rent & Payments | Governor (gateway/payouts) | Driver (collect/income) | Responder (pay/balance) | Owner → Tenant |
| M10 Maintenance | Governor (SLA/escalation) | Driver (approve/assign/close) | Responder (report) | Tenant → **Owner** |
| M11 Contractors | Governor (registry/ratings) | Driver (hire/assign) | Monitor (job done) | Owner → Contractor* |
| M12 Subscriptions | **Driver (plans/billing)** | Responder (pay/subscribe) | - | Admin → Owner |
| M13 Featured/Ads | **Driver (pricing/approve)** | Responder (buy) | sees badges | Admin → Owner → Tenant |
| M14 Verification | **Driver (verify)** | Responder (submit evidence) | trusts (badges) | Admin ↔ Owner |
| M15 Notifications | configures/globals | receives | receives | all |
| M16 Landlord Dashboard | - | **Driver (operate)** | - | Owner only |
| M17 Reports | **Driver (platform)** | Driver (own portfolio) | light (own activity) | Admin + Owner |
| M18 Admin Console | **Driver (everything)** | data source | data source | Admin |
| M19 Disputes | **Driver (resolve)** | Responder (defend) | Responder (report/appeal) | any → Admin |
| M20 Documents | **Driver (registry/retention)** | uploader | uploader | Admin + all |

\* M11 introduces a **fourth actor** (Contractor). Flag it in the UI narration: Contractor is not a platform subscriber; they are registered users whose scope is `contractor` workflows only.

### Coupling rationale for "must be built at once" groups

| Coupled group | Reason they cannot be split |
|---|---|
| M2 + M3 + M4 | One continuous content pipeline. Marketplace needs seeded, real listings to test filtering/cards; favourites need cards to attach to. Splitting leaves M3 and M4 impossible to test. |
| M5 + M6 | Identical shared scaffold (thread + statuses + notifications + row-level isolation per owner). Duplicating it later is the single most wasteful rework in the plan. |
| M7 + M8 + M20 | A single acceptance path: apply → approve → lease → signed → occupied → document stored. Stopping at "approval" leaves dead state with no next step. |
| M24 + M9 + M12 + M13 | One configuration-driven billing brain. The configuration engine (Module 24) is the **prerequisite**: grace, proration mode, suspension behaviour, invoice/receipt numbering, cycle days, renewal reminders, payment/POP/approval, late fees and featured pricing are data read through `ConfigurationService` — no billing code is written before it exists. Gateway + ledger are then shared by rent, subscription and ad money; all reconcile from one ledger. |
| M10 + M11 | Maintenance without an assignee is a to-do list, not a workflow. |
| M14 + M19 + M18 + M17 | Trust layer. Badge holds are a dispute outcome; dispute reporting needs the evidence store; both need the admin queue; their effectiveness must be measurable. |

## 4. Wave-by-Wave Vertical Slices

Each wave: scope, end-to-end walk, and the exact feature tests required. **Finish = all steps done + all tests green + no loose ends.**

### Wave 0 - Foundation (DONE)

Auth by email/username, lockout, password expiry/forced change, roles + pivots, `admin`/`role` middleware + 403, role-redirect login, Dzimba layouts, marketplace shell, Admin/Owner/Tenant dashboards, `properties` + `property_favourites` + `rental_applications` tables, access test suite.

### Wave 1 - List & Discover (M2 + M3 + M4)

**End-to-end walk:** owner creates a listing (migration already exists; add gallery upload) → listing validates and publishes → marketplace lists it with filters working → tenant opens detail → tenant saves favourite → owner dashboard shows the saved-count and latest listings.

| Feature | End-to-end steps | Tests required per feature |
|---|---|---|
| Property CRUD (Owner) | DB (gallery/images table; zone + building_size + land_size on properties) → model → `PropertyService` + controller + validation → routes under `role:Owner` → Owner pages → StatusBadge reuse | create; update; delete; only-own-properties 403/404 for other owners; validation errors |
| Listing publish & status | status transitions (available/reserved/occupied/unavailable) with history | transition allowed matrix; reserved/occupied blocked for for others; history row written |
| Marketplace search | index query + filters + sort + pagination → public page + cards (card anatomy per `docs/modules/03` market review) | filter combos (type, zone/city, price range min+max, beds, baths, furnished); only `available` shown; featured-first sort; pagination bounds |
| Property detail page | single-property read → gallery, amenities, owner info, CTAs | 200 for public; 404 for hidden/unavailable; correct owner info |
| Favourites (M4) | pivot CRUD → toggle on card + detail + "My Favourites" page | toggle on/off idempotent; unique constraint; list correct; guest→login redirect |

**Wave-tests gate:** `php artisan test` (new suite `ListingAndDiscoverTest`) + route:list has only live routes + no dead imports/build-clean.

### Wave 2 - Engage (M5 + M6) ✅ COMPLETE

**End-to-end walk:** tenant enqueues → owner inbox shows grouped by property → owner replies → tenant sees thread → tenant requests viewing → owner accepts → slot locked → both parties see confirmations. Notifications (in-app) raised on enquiry created/replied and viewing requested/accepted/rescheduled.

| Feature | End-to-end steps | Tests required per feature |
|---|---|---|
| Enquiry create/inbox | ✅ enquiry table → service + validation → routes → owner inbox + tenant thread | ✅ create; owner-only inbox isolation; status transitions new/read/replied/closed; reply append |
| Viewing slots | ✅ slots CRUD (owner) | ✅ owner creates/edits slots; slot date validation; owner/slot isolation 404 |
| Viewing requests | ✅ request on slot → accept/reschedule/cancel → reminders flagged | ✅ accept locks slot (double-book 409); reschedule path; cancel path; no-show marking; isolation 404s |
| Notifications in-app (M15 start) | ✅ bell + `notifications` rows on enquiry/viewing events, read/unread + deep-link | ✅ event creates row; read/unread; deep-link; isolation |

**Wave-tests gate:** `EngageTest` suite green; access tests extended for `/owner` inbox and `/tenant` threads; full suite green.

### Wave 3 - Commit (M7 + M8 + M20)

**End-to-end walk:** tenant applies → owner shortlists → owner approves → property `reserved` → other applications auto-rejected → owner generates lease from application → both sign → lease `active` + property `occupied` → lease document in store.

| Feature | End-to-end steps | Tests required per feature |
|---|---|---|
| Apply (M7) | ✅ application create + validation + tenant tracking page | ✅ double-apply blocked; property-must-be-available; message length |
| Review/approve/reject | ✅ owner review screen; approve/reject/shortlist | ✅ exclusivity (one approved); reject reason recorded; terminal immutability; isolation 404s — ⏳ auto-reject others + status→reserved deferred to lease slice |
| Lease create (M8) | ✅ generate from approved application → lease_no → draft | ✅ only-from-approved-application; data prefilled correctly; no double lease; cross-owner 404; **property → reserved + auto-reject rivals (FR-05 handover landed)** |
| Signing (M8) | ✅ two-party signature flow → active | ✅ signed only after both; state active; property → occupied; parties-only 404s; no double sign |
| Renew (M8) | ✅ owner renews an active lease → new draft linked to original → signing activates + marks source renewed | ✅ only-active renewable; terms copied; cross-owner 404; one in-flight renewal; renewed lease not renewable; signing activates renewal + source → renewed |
| Document store (M20-lite) | ✅ auto-stored agreement snapshot on lease activation (`documents` row + version) | ✅ file stored at activation; accessible to owner + tenant; strangers 404 (view + download); admin access; scoped indexes |

**Wave-tests gate:** `CommitTest` suite green; the pipeline test (apply→lease→occupied) asserted in ONE feature test; full suite green.

### Wave 4 - Monetise (M24 + M9 + M12 + M13)

**End-to-end walk:** staff open the ⚙️ Configuration Centre and change GRACE/PRICE/SUSPENSION/PRORATION/numbering data (no code) → owner subscribes (quota enforced on publish from Wave 1) → rent schedule auto-generated from active lease → invoice issued → tenant pays → invoice `paid` + receipt → arrears computed → owner buys featured placement → placement window promotes listing.

| Feature | End-to-end steps | Tests required per feature |
|---|---|---|
| Configuration engine (M24) | `system_configurations` + `configuration_audits` migrations → typed, cached, audited `ConfigurationService` (get/getGroup/set/reset; high/critical risk needs `approved_by`) → seeded billing defaults → admin ⚙️ Configuration Centre (billing group) → Feature Catalogue tables + plan grants | change-config-is-audited (old/new/by/at/why); approval gate for high-risk keys; cache invalidation on set; **change grace/price/proration/numbering in config → engines use the new value with zero code change; existing invoices/cycles untouched** |
| Plans/subscribe (M12) | ✅ plans CRUD (admin) + subscribe + quota hook + Feature Catalogue grants | quota enforced at publish; pro-rata upgrade (proration mode from config); downgrade deferred; grace→suspend (grace days + suspension behaviour from config); archived plan rejected |
| Plans/subscribe (M12) config conformance | ✅ done (slice 2): code constants removed (`GRACE_DAYS`, cycle-day map, SUB-/RCT- numbering, over-limit copy all read `ConfigurationService`); `upgrade()` honours `subscriptions.proration.mode` incl. `apply_at_renewal` deferral (`pending_upgrade_to`); suspension behaviour `keep_listings` enforced (gate new publishes) | values read from DB config; changing config changes behaviour; old invoices unchanged; test suite still green |
| Rent schedule (M9) | ✅ done (slice 3): `rent_schedules` + `rent_invoices` (state machine with `paid`/`refunded` landing with the payment slice) + `RentService` (config-driven timing/reminders/numbering) + `rent:process` + `owner.rent.index`/`tenant.rent.index` pages + sign-hook + demo schedule | exact invoice count for term; amounts match lease; period boundaries |
| Payment (M9) | ✅ done (slice 4): `payments` ledger (pending→settled/rejected state machine) on real invoice FKs → bank/mobile/cash/online manual entry (`payments.methods`) → exact-amount + idempotent settle (or staff-approve queue when POP/config-form threshold met) → invoice `paid` + unique `RCT-` receipt + tenant notified → plain-text receipt download (payer/admin only) | idempotency (no double-pay); receipt unique; POP approval threshold from config; cross-tenant 404s (slice tests) |
| Arrears & income | aggregation service → landlord dashboard | arithmetic exactness vs ledger fixture |
| Ads (M13) | package purchase → placement window → featured sort in M3 | window enforcement; one-placement rule; prorated refund on cancel; pricing/duration from config |

**Wave-tests gate:** `MonetiseTest` suite green; configuration-engine tests prove "rules are data" (AC-05); money exactness tests (decimal, no float); full suite.

### Wave 5 - Operate (M10 + M11)

**End-to-end walk:** tenant reports leak → owner approves + quotes → contractor assigned → contractor works (status in_progress) → completed → owner inspects + closes → rated.

| Feature | End-to-end steps | Tests required per feature |
|---|---|---|
| Report & triage (M10) | request create + priority + SLA counter | emergency notifies admin; priority escalation timer |
| Assignment (M11) | contractor registry + assign to job | only verified assignable; double-assign blocked |
| Track & close | ✅ status transitions + tenant confirm → closed | ✅ transition matrix; closed feeds maintenance spend (resolved_at + quote retained) |
| Ratings | ✅ post-completion rating → recompute | ✅ rating-only-after-completed; average correctness (OperateTest 37) |

**Wave-tests gate:** `OperateTest` suite green (37 tests); full suite green (403 passed).

### Wave 6 - Trust & Govern (M14 + M19 + M18 + M17)

**End-to-end walk:** owner submits evidence → admin verifies → badge → tenant filters verified-only → a listing gets reported → badge holds → admin investigates → resolves → report is measurable in platform analytics.

| Feature | End-to-end steps | Tests required per feature |
|---|---|---|
| Verification (M14) | case + evidence docs + badge state + verified-only filter | badge reflects latest decision; evidence never public; revoke demotes |
| Disputes (M19) | report/dispute create → evidence attach → admin decision + action log | full action history; badge hold on report; restore on dismissal |
| Admin console completion (M18) | queues for verify/dispute/review + audit | admin-role matrix 403s; audit rows written |
| Reports (M17) | platform + owner aggregates + export | reconciles with ledgers; owner isolation; CSV export shape |

**Wave-tests gate:** `TrustGovernTest` suite green; full suite; access matrix fully covered.

## 5. Test-After-Every-Feature Protocol (MANDATORY)

Every feature, in this order, before it counts as done:

1. **Write the feature test first** if behaviour is already clear (Test-last accepted when the slice is exploratory, but the test is still written in the same sitting).
2. **Happy path test** - the main success journey.
3. **Negative test** - the guard (e.g. wrong role → 403, wrong owner → 404, invalid input → 422).
4. **State-transition / money test** - whenever the feature changes a status or an amount (exclusivity, idempotency, exactness).
5. **Permission-matrix test** - the new route appears in the role matrix suite so it can never be forgotten later.
6. **Run the feature suite**: `php artisan test --filter=<SuiteName>`.
7. **Run the full suite**: `php artisan test`.
8. **Front-end changed?** `npm run build` must pass with no unused-import warnings; typecheck clean.
9. **Routes changed?** `php artisan route:list` - confirm no duplicate/orphan route names.
10. **DB changed?** `php artisan migrate:fresh --seed` on the dev DB, then re-run the full suite.

**Definition of "No loose ends" (every feature and every wave):**

- No route without a controller, no controller without a route.
- No page added to navigation that 403s for its intended role.
- No TODO/FIXME/TBD marker left.
- No commented-out code, dead imports, unused props or orphaned components.
- No new role-gated route absent from the access-control tests.
- Demo seed data updated if a feature changes.

## 6. Sequencing Guardrails

1. **Never start Wave N+1 while Wave N has open tests.** Untested code freezes the wave.
2. **Coupling over convenience.** M5 and M6 are built together even though "enquiries" alone would ship faster; the second flow would cost more later.
3. **The marketplace is the proof point.** Waves are only signed off when demoable in the browser for all three parties, not when the backend passes tests.
4. **M15 and M16 grow with every wave.** Do not build them "fully" in Wave 1; extend them as each new event type and data source lands.
5. **Estimates (indicative, single developer):** Wave 1 ≈ 3-4 weeks, Wave 2 ≈ 2-3 weeks, Wave 3 ≈ 3-4 weeks, Wave 4 ≈ 4-5 weeks, Wave 5 ≈ 2-3 weeks, Wave 6 ≈ 2-3 weeks. These are stretch targets and each wave's gates are the source of truth.

## 7. Definition of Done (every module, unchanged)

1. Migrations + models + relationships.
2. Seed data where needed.
3. Controllers + route group scoped by role.
4. Inertia page + shared components (StatusBadge etc.).
5. Feature tests: happy path + negative (403) + permission matrix.
6. Module doc in `docs/modules/` updated and cross-referenced.
7. `docs/modules/23-implementation-plan.md` **§1.5 status tracker updated** (next-up feature ticked off, next feature promoted) and wave status ticked.
8. AGENTS.md global rules satisfied (no loose ends, standards checked, tests run).